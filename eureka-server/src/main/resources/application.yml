server:
  port: 8761

spring:
  application:
    name: eureka-server
  cloud:
    service-registry:
      auto-registration:
        enabled: false

eureka:
  client:
    service-url:
      registerWithEureka: false

logging:
  level:
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF
